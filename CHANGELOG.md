# Changelog

All notable changes are recorded here. SkillGate follows semantic versioning after the first public release.

## Unreleased

- Add a pinned 30-task QuixBugs external execution protocol with MIT/source provenance, deterministic selection, full oracle preflight, a 360-run schedule, and an auditable protocol lock.
- Freeze exact executor models and cost budget, add sanitized real Codex/OpenCode smoke captures with private raw hash linkage, and make both execution protocols run-ready while preserving negative paper gates.
- Add a hash-verifying, resumable schedule-cell runner with hidden-oracle finalization, strict result rows, cost/timeout/instrumentation fields, networkless OpenCode shell isolation, and offline outcome/exclusion conformance.
- Add campaign preflight and immutable private cost reconciliation; unresolved USD costs or prior per-run overages now stop new live cells before state creation.
- Bind collection-time campaign cost disclosure to schedule, results, and private ledger hashes, and require the validated disclosure for real experiment analysis.
- Harden CI with frozen dependency installs, full-SHA-pinned official actions, and the canonical schema-count smoke gate; add a signed-tag GitHub Release workflow with clean builds, full audits, provenance attestations, and environment approval.
- Add a cited related-work survey, bounded novelty positioning against specification inference and coding-agent evaluation, and an artifact gate for dangling or inconsistent references.

- Upgraded the external issue candidate corpus to v2 with immutable pre-resolution snapshots, commit-pinned license evidence, curator-only resolution reconstruction, and leakage-aware validation.
- Added deterministic two-curator packet generation, a strict completed-label schema, anonymized disagreement analysis, and fail-closed gold adjudication for external candidates.
- Added exact external snapshot materialization, source-free public locks, compiler-smoke and resolution-leakage audits, exposing a 1/3 task-kind diagnostic on the staging set.
- Added TaskBrief-bound execution traces, fail-closed scope and forbidden-action auditing, byte-bound verification evidence, false-success detection, and a five-case synthetic conformance suite.
- Added a working manuscript and deterministic automated paper-evidence bundle with a fail-closed claim-status registry.
- Added fail-closed Codex/OpenCode JSONL adapters, frozen plan/report schemas, and parser conformance fixtures.
- Added a machine-validated downstream execution pilot preregistration with oracle preflight, deterministic scheduling, and explicit negative readiness gates.
- Added schedule-bound execution-result analysis with clustered bootstrap, sign-flip tests, Holm correction, and synthetic-null conformance.

## 0.1.0 - 2026-06-18

- Added the rules-only P0 TaskBrief compiler and evidence-backed artifact set.
- Added P1 clarification, atomic multi-answer recording, secret redaction, and parent-child provenance.
- Added public/private benchmark splitting, normalized annotation packets, analysis, and release tooling.
- Added issue-style benchmark provenance, benchmark cards, stability evaluation, JSON Schemas, and artifact audits.
- Added release-readiness validation for the Python distribution and research materials.
